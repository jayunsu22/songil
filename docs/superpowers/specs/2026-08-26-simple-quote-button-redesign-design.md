# 간편견적 진입 UX 개편 — 설계 문서

**날짜:** 2026-08-26
**대상 저장소/브랜치:** `jayunsu22/songil`, 작업 브랜치 `feature/simple-quote-button` (main에서 분기)
**대상 파일:** `index.html`, `index_app.js`, `index_style.css` (1. 웹_배포파일 (GitHub) 폴더)

## 배경

현재 라이브 페이지(`1film.co.kr/songil`)는 첫 방문 시 웰컴 카드 아래에 `평형별 / 품목별 / 사진견적` 3탭 간편견적 패널이 강제로 펼쳐진 채(inline) 노출되고, 대화가 한 번이라도 시작되면 그제서야 `🛠️ 간편견적 열기`라는 가로 전체폭 배너 버튼으로 바뀌어 채팅 입력창 바로 위에 끼어든다.

한편 채팅창에 사진 없이 텍스트만("화장실문2개, 샤시2개" 등) 입력해 전송해도 이미 정상적으로 견적이 계산되어 나온다(`index_app.js`의 `sendRequest()`가 이미지 유무와 무관하게 텍스트만으로도 동일하게 `CONFIG.estimateUrl`에 전송함). 즉 백엔드(n8n)나 프론트의 전송 로직은 손댈 필요가 없고, 문제는 이 방식이 화면에서 전혀 안내되지 않아 아무도 쓰지 않는다는 점과, 탭 패널이 화면을 과도하게 차지한다는 UX 문제다.

## 목표

1. `평형별/품목별/사진견적` 패널은 없애지 않고 유지하되, **항상(첫 방문 포함) 우측 하단의 작은 아이콘 버튼**으로 축소해서 채팅 입력창을 가리지 않게 한다.
2. 이미 작동하는 채팅 텍스트 견적 입력 방식을 사용자가 알아채고 쓸 수 있도록 **웰컴 카드/입력창에 안내 문구**를 추가한다.
3. 지금의 라이브 상태는 그대로 보존하고, 작업은 별도 브랜치에서 진행한 뒤 확인 후 main에 반영한다.

## 비목표 (건드리지 않는 것)

- n8n 워크플로우, 견적 계산 로직, Airtable 연동 — 변경 없음
- `평형별/품목별/사진견적` 패널 내부 동작(장바구니, 평면도 핀 견적 등) — 변경 없음
- `sendRequest()`의 전송/파싱 로직 — 변경 없음 (이미 텍스트만으로 정상 동작 확인됨)
- **30초 카운트다운 로딩 화면(원형 타이머 애니메이션)** — 변경 없음, 지금 이미지/애니메이션 그대로 사용
- **견적 산출 후 결과 화면** — 변경 없음

## 변경 사항

### 1. 진입 UI: 좌측 벽 탭 + 슬라이드 드로어

- `index_app.js`의 `renderQuickQuoteModal()`에 있는 `isInline = (chatHistory.length === 0)` 분기를 제거한다. 첫 방문이든 대화 중이든 항상 닫힌 상태(탭만 보임)로 시작 — 인라인 강제 노출(`quick-quote-inline-container`)은 더 이상 만들지 않는다.
- `addOpenQuickQuoteButton()`이 만드는 진입 버튼을 화면 우측 하단 배너/버튼에서 **화면 왼쪽 벽에 붙은 작은 세로형 탭(손잡이)**으로 바꾼다:
  - 닫힘 상태: `position: fixed; left: 0; top: 50%; transform: translateY(-50%);` 좁은 세로 탭(아이콘 + "간편견적" 세로 텍스트), 화면 왼쪽 벽에 절반쯤 파묻힌 듯 붙어있음. 채팅 입력창과 절대 겹치지 않음(입력창 영역 위쪽에 위치).
  - **탭(tap) 동작**: 손잡이를 누르면 왼쪽에서 오른쪽으로 패널이 슬라이드 인(`transform: translateX(-100%) → translateX(0)`, ease-out 트랜지션)하며 화면의 약 78~85% 폭을 덮는 드로어(서랍) 형태로 열림. 뒤쪽은 반투명 배경(backdrop)으로 어둡게 처리.
  - 닫기: 드로어 안의 X 버튼, 또는 배경(backdrop) 탭, 또는 손잡이 재탭으로 닫힘 — 슬라이드 아웃 후 다시 왼쪽 탭 상태로 복귀.
  - **PC/모바일 동일하게 탭(클릭) 한 번으로 열고 닫음** (드래그 제스처 아님, 호버 아님).
  - 드로어 내부 콘텐츠(평형별/품목별/사진견적 3탭, 장바구니 등)는 기존 모달 바디 렌더링 로직(`renderModalBody`)을 그대로 재사용 — 내부 로직 변경 없음, 여닫는 컨테이너의 위치/애니메이션만 바뀜.
- `renderWelcomeIfNeeded()`가 렌더링하는 웰컴 카드 자체(인사말 + 3개 원형 아이콘 장식)는 유지하되, 그 아래에 있던 인라인 패널 강제 노출만 없앤다.

### 2. 발견성: 채팅 텍스트 입력 안내

- 웰컴 카드(`renderWelcomeIfNeeded()`)에 안내 문구 한 줄 추가: 예) "💬 또는 채팅창에 바로 '화장실문2개, 샤시2개'처럼 입력해보세요!"
- 채팅 입력창(`index.html`의 `#userInput`) placeholder 텍스트를 예시가 담긴 문구로 변경 (예: "예: 화장실문2개, 샤시2개").

### 3. Git 브랜치 전략 (백업 겸용)

- 현재 `main` HEAD에 태그 `backup-live-20260826` 생성 후 GitHub에 push — 언제든 이 시점 코드로 되돌릴 수 있는 고정 스냅샷.
- 작업은 `feature/simple-quote-button` 브랜치에서 진행. 완료 후 로컬/모바일에서 테스트 → 문제 없으면 `main`으로 병합 후 push(배포). 마음에 안 들면 브랜치를 버리면 라이브(`main`)는 전혀 영향 없음.

## 구현 메모

- 위험 최소화를 위해 기존 `.quick-quote-modal` / `.quick-quote-modal-content` DOM 구조와 클래스는 그대로 재사용하고, **CSS만 바꿔서** 가운데 정렬 모달 → 왼쪽에서 슬라이드하는 드로어로 전환한다 (`.quick-quote-modal-content`에 `transform: translateX()` 트랜지션 적용, 폭을 78~85%로 제한). `floating-cart-badge` 등 기존에 `.quick-quote-modal-content`를 셀렉터로 참조하는 코드는 그대로 동작해야 한다.
- 왼쪽 벽 탭은 `addOpenQuickQuoteButton()` 함수 내부의 버튼 생성/스타일 부분만 교체하고, 클릭 핸들러(모달 여는 로직)는 그대로 재사용한다.

## 테스트 체크리스트 (기존 `시스템_구조_및_체크리스트.md` 기반 + 신규 항목)

- [ ] 기존 체크리스트 1번(가맹점 접속 잠금), 5번(전송 버튼 널 체크), 9번(중문짝 단가) 등 회귀 없는지 확인
- [ ] 첫 방문 시 웰컴 카드만 보이고, 3탭 패널은 인라인으로 펼쳐지지 않는지 확인
- [ ] 왼쪽 벽 탭이 모바일(375px) 화면에서 채팅 입력창과 겹치지 않는지 확인
- [ ] 탭을 누르면 드로어가 왼쪽에서 슬라이드로 열리고, 배경(backdrop)/X버튼/탭 재클릭으로 정상적으로 닫히는지 확인
- [ ] 드로어 내부의 평형별/품목별/사진견적 탭 및 장바구니 기능이 기존과 동일하게 동작하는지 확인
- [ ] 채팅창에 "화장실문2개, 샤시2개" 같은 텍스트만 쳐서 전송 시 정상적으로 견적이 산출되는지 확인 (회귀 확인용, 기존에 되던 것)
- [ ] 30초 카운트다운 로딩 화면과 견적 결과 화면이 기존과 동일하게 나오는지 확인 (변경 없음 재확인)
- [ ] 장바구니 담은 뒤 뜨는 `floating-cart-badge`가 드로어 안에서 정상적으로 겹치지 않고 표시되는지 확인

## 롤백 방법

`backup-live-20260826` 태그로 체크아웃하거나, `feature/simple-quote-button` 브랜치를 main에 병합하지 않고 폐기하면 라이브 상태는 전혀 변경되지 않는다.
